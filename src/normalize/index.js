import { normalizeToNumber } from './normalize-value';
import { normalizeDate } from './normalize-date';
import { normalizeLabel } from './normalize-label';

export {
  normalizeDate,
  normalizeToNumber,
  normalizeLabel,
};